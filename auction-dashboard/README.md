# Auction Dashboard API

This is a dummy backend for an auction dashboard project.

## Features
- GET /api/auctions → returns all auctions
- GET /api/surveys → returns survey responses
- POST /api/surveys → submit a new survey response
- GET /health → basic server status

All data is mocked and stored locally for demonstration purposes.


## Setup
1. Install dependencies:
